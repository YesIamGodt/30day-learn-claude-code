---
title: AI 的记忆：对话历史
objectives:
  - 理解为什么 AI 没有记忆
  - 实现对话历史管理
  - 学会给 API 传递历史消息
---

# Day 7: AI 的记忆：对话历史

## 这一节要解决什么问题？

每次给 AI 发消息，它都不知道之前说过什么。因为每次 API 调用是独立的。要让 AI 记住上下文，需要**自己维护历史消息**，每次调用都带上。

## 为什么 AI 自己不记？

成本问题！AI 模型每次处理的内容越多，费用越高。维护历史是**调用方**的责任。

## 代码演示

```js
class ConversationHistory {
  constructor(maxMessages = 20) {
    this.messages = [];
    this.maxMessages = maxMessages;
  }

  // 添加用户消息
  addUser(content) {
    this.messages.push({ role: "user", content });
    this._trim();
  }

  // 添加 AI 回复
  addAssistant(content) {
    this.messages.push({ role: "assistant", content });
    this._trim();
  }

  // 获取带历史的消息
  getMessages() {
    return this.messages;
  }

  // 清空历史
  clear() {
    this.messages = [];
  }

  // 超过上限时，保留最新的消息
  _trim() {
    if (this.messages.length > this.maxMessages) {
      // 保留系统提示 + 最新的消息
      const system = this.messages.find(m => m.role === "system");
      const rest = this.messages.slice(-(this.maxMessages - (system ? 1 : 0)));
      if (system && !rest.includes(system)) {
        this.messages = [system, ...rest];
      } else {
        this.messages = rest;
      }
    }
  }
}

// 使用示例
const history = new ConversationHistory(10);

history.addUser("我叫小明");
history.addAssistant("你好小明！很高兴认识你。");
history.addUser("我刚才说了什么？");

console.log("发送的消息历史:");
console.log(JSON.stringify(history.getMessages(), null, 2));
```

## 知识点

- AI API 只认 `{ role: "user" | "assistant" | "system", content: "..." }` 格式
- **自己维护历史**，AI 本身不存储任何东西
- `_trim()` — 超过上限时裁剪旧消息，保留最近的上下文
- `maxMessages` — 控制历史长度，避免超出 AI 的上下文窗口

## 动手练习

添加一个 `history.save()` 方法，把历史保存到 localStorage（浏览器环境）。
