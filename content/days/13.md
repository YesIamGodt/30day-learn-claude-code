---
title: 错误处理：出错了怎么办？
objectives:
  - 掌握 try/catch 错误处理模式
  - 实现重试机制
  - 学会给用户友好的错误提示
---

# Day 13: 错误处理：出错了怎么办？

## 这一节要解决什么问题？

调用 AI API、网络请求、文件操作——都可能出错。好的错误处理让程序不会崩溃，还能给用户有用的反馈。

## 错误分类

```
网络错误（断网、超时）
  ↓
API 错误（Key 无效、频率限制、模型不可用）
  ↓
工具错误（文件不存在、权限不足）
  ↓
AI 响应错误（格式错误、超时）
```

## 代码演示

```js
// 重试装饰器
async function withRetry(fn, maxRetries = 3, delayMs = 1000) {
  for (let attempt = 1; attempt <= maxRetries; attempt++) {
    try {
      return await fn();
    } catch (error) {
      const isRetryable = ["ECONNRESET", "ETIMEDOUT", "429"].some(
        code => error.message.includes(code)
      );

      if (!isRetryable || attempt === maxRetries) {
        throw error; // 不可重试的错误，或已达最大次数
      }

      console.log(`⚠ 第 ${attempt} 次尝试失败: ${error.message}`);
      console.log(`   ${delayMs * attempt}ms 后重试...`);
      await new Promise(r => setTimeout(r, delayMs * attempt));
    }
  }
}

// 带错误分类的 API 调用
class APIError extends Error {
  constructor(message, code, status) {
    super(message);
    this.code = code;
    this.status = status;
  }
}

async function 安全调用AI(messages, model) {
  try {
    const response = await withRetry(async () => {
      const res = await fetch("https://api.anthropic.com/v1/messages", {
        method: "POST",
        headers: { "x-api-key": process.env.ANTHROPIC_API_KEY },
        body: JSON.stringify({ model, messages, max_tokens: 1024 }),
      });

      if (!res.ok) {
        const body = await res.json().catch(() => ({}));
        throw new APIError(
          body.error?.message ?? `HTTP ${res.status}`,
          body.error?.type ?? "unknown",
          res.status
        );
      }

      return res.json();
    });

    return response;
  } catch (e) {
    if (e instanceof APIError) {
      if (e.status === 401) throw new Error("API Key 无效，请检查 ANTHROPIC_API_KEY");
      if (e.status === 429) throw new Error("请求过于频繁，请稍后再试");
      throw new Error(`AI 调用失败: ${e.message}`);
    }
    throw new Error(`未知错误: ${e.message}`);
  }
}

// 测试错误分类
console.log("错误处理架构演示完成。");
console.log("实际运行会发起真实 API 调用。");
```
