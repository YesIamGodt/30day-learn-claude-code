---
title: 工具调用循环：AI 怎么做决策？
objectives:
  - 理解 ReAct 循环（思考→行动→观察→再思考）
  - 实现工具调用引擎
  - 理解 AI 如何决定调用哪个工具
---

# Day 8: 工具调用循环：AI 怎么做决策？

## 这一节要解决什么问题？

AI 不是一次调用就完成任务的。它需要：思考 → 决定用哪个工具 → 执行 → 看结果 → 再思考 → ……这个循环叫做 **ReAct**（Reason + Act）。

## ReAct 循环图解

```
用户: "帮我看看 package.json 有什么依赖"

AI 思考: 用户想看文件 → 调用 FileRead 工具
    ↓
工具执行: 读取 package.json
    ↓
AI 看到结果: 依赖列表
    ↓
AI 思考: 已获取文件内容 → 直接回复用户
    ↓
完成！
```

## 代码演示

```js
class QueryEngine {
  constructor(client, history, registry) {
    this.client = client;
    this.history = history;
    this.registry = registry;
    this.maxIterations = 10;
  }

  async run(userInput) {
    this.history.addUser(userInput);

    for (let i = 0; i < this.maxIterations; i++) {
      // 1. 让 AI 思考并决定是否调用工具
      const response = await this.client.messages.create({
        model: "claude-sonnet-4-7-20250514",
        max_tokens: 1024,
        messages: this.history.getMessages(),
        tools: this.registry.list().map(t => t.toSchema()),
      });

      const stopReason = response.stop_reason;
      const content = response.content[0];

      // 2. 如果是最终回复，直接返回
      if (stopReason === "end_turn") {
        const text = content.text;
        this.history.addAssistant(text);
        return text;
      }

      // 3. 如果是工具调用，执行它
      if (stopReason === "tool_use") {
        const toolName = content.name;
        const toolInput = content.input;

        console.log(`→ 调用工具: ${toolName}`);
        const tool = this.registry.get(toolName);
        const result = await tool.execute(toolInput);

        // 4. 把工具结果加回历史，让 AI 继续思考
        this.history.addAssistant(
          JSON.stringify({ type: "tool_result", name: toolName, content: result })
        );
        continue;
      }
    }

    return "执行超时，请重试。";
  }
}

console.log("QueryEngine 架构演示完成。");
console.log("实际运行需要真实的 API Key。");
```

## 知识点

- **ReAct 循环**：Reason（推理） + Act（行动）循环
- `stop_reason` — AI 停止的原因：`end_turn`（回复用户）还是 `tool_use`（调用工具）
- 工具结果要加回历史，否则 AI 不知道工具返回了什么
- `maxIterations` — 防止死循环（工具A调工具B调工具A……）

## 动手练习

把 `maxIterations` 改成 5，运行看看超过次数会怎样。
