---
title: 工具箱设计：怎么管 40 个工具？
objectives:
  - 理解什么是"注册表模式"
  - 学会用统一的接口定义工具
  - 理解插件式架构的好处
---

# Day 4: 工具箱设计：怎么管 40 个工具？

## 这一节要解决什么问题？

Claude Code 有 40+ 工具：Bash、读写文件、搜索网页、读图片……这么多工具，怎么管理？

答案：**统一的"工具箱"接口**。就像宜家的工具箱，不管是什么工具（螺丝刀、锤子），都遵守同一个规则：有一个名字，能被放进箱子，能被取出来用。

## 代码演示

```js
// 工具的通用接口（JS 里用约定代替抽象基类）
// 每个工具都要有：name、description、execute()

class BaseTool {
  get name() { throw new Error("子类必须实现 name"); }
  get description() { throw new Error("子类必须实现 description"); }
  async execute(input) { throw new Error("子类必须实现 execute"); }
}

// 工具箱（注册表）
class ToolRegistry {
  constructor() {
    this._tools = new Map();
  }

  // 注册工具
  register(tool) {
    if (this._tools.has(tool.name)) {
      throw new Error(`工具 ${tool.name} 已注册`);
    }
    this._tools.set(tool.name, tool);
  }

  // 获取工具
  get(name) {
    if (!this._tools.has(name)) {
      throw new Error(`工具 ${name} 未注册`);
    }
    return this._tools.get(name);
  }

  // 列出所有工具
  list() {
    return Array.from(this._tools.values());
  }
}

// 创建一个具体工具
class HelloTool extends BaseTool {
  get name() { return "hello"; }
  get description() { return "返回问候语"; }
  async execute(input) {
    return `Hello, ${input}!`;
  }
}

// 使用
const registry = new ToolRegistry();
registry.register(new HelloTool());

console.log("已注册工具:", registry.list().map(t => t.name));
const result = await registry.get("hello").execute("Claude");
console.log("执行结果:", result);
```

## 知识点

- **注册表模式**：全局有一个 Map，按名字注册和查找工具
- **统一接口**：不管什么工具，都有 `name`、`description`、`execute()`
- **插件式架构**：新增工具不需要改核心代码，只管注册就行
- JS 没有抽象基类，用 `throw new Error()` 约定"子类必须实现"

## 动手练习

仿照 `HelloTool`，创建一个 `EchoTool`，输入什么返回什么（`input => input`）。
