---
title: 让 AI 会搜索网页
objectives:
  - 理解 WebFetch 工具
  - 学会用 fetch 获取网页内容
  - 理解 AI 为什么需要联网工具
---

# Day 6: 让 AI 会搜索网页

## 这一节要解决什么问题？

AI 的知识有截止日期。要让它查实时信息，需要给它装一个"网页读取"工具。

## 代码演示

```js
// 网页读取工具
class WebFetchTool {
  get name() { return "web_fetch"; }
  get description() { return "获取指定 URL 的网页内容"; }

  async execute(url) {
    if (!url.startsWith("http")) {
      throw new Error("URL 必须以 http:// 或 https:// 开头");
    }

    const response = await fetch(url, {
      headers: {
        "User-Agent": "Mozilla/5.0 (compatible; ClaudeCode-Tutorial/1.0)"
      }
    });

    if (!response.ok) {
      throw new Error(`网页获取失败: ${response.status} ${response.statusText}`);
    }

    const text = await response.text();

    // 简化输出（去掉 HTML 标签，只保留正文）
    const bodyMatch = text.match(/<body[^>]*>([\s\S]*?)<\/body>/i);
    const body = bodyMatch ? bodyMatch[1] : text;
    const plainText = body
      .replace(/<[^>]+>/g, " ")
      .replace(/\s+/g, " ")
      .trim();

    return plainText.slice(0, 2000); // 限制长度
  }
}

// 测试
(async () => {
  const tool = new WebFetchTool();
  try {
    // 读取一个简单的网页
    const content = await tool.execute("https://example.com");
    console.log("网页内容（前200字）:", content.slice(0, 200));
  } catch (e) {
    console.error("失败:", e.message);
  }
})();
```

## 知识点

- `fetch(url)` — 内置 HTTP 请求（Node.js 18+）
- `response.ok` — 检查 HTTP 状态码是否成功（200-299）
- HTML 标签去掉 → 纯文本：用正则把 `<...>` 替换为空格
- 限制返回长度：AI 的上下文窗口有限，不需要整页内容

## 动手练习

把 URL 改成 `https://httpbin.org/html`，看看返回什么。
